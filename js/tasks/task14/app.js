let arr = ['sen', 'men', 'o', 17, 4, { id: 1, username: 'Bret', email: "incere@april.biz" }]
const netice = arr.map((elem) => {
    if (elem == {}) {
        return elem
    }
})
console.log(netice);

const cavab = arr.forEach((elem) => {
    for (let i = 1; i < elem; i++) {
        if(elem ){

        }
    }
})
document.write(cavab);
